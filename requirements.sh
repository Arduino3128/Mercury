pip3 install colorama selenium mechanize pygoogling
# optparse,haslib, smtplib and platform  modules are already installed in pyhton3
# by default so it will be there when you install(update) python3
